{
  "name": "manta",
  "version": "1.0.0",
  "description": "Local LLM-assisted RSS/feed filtering app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "server": "npm run dev --workspace server",
    "web": "npm run dev --workspace web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "server",
    "web"
  ],
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
